@{
    AllNodes = @(
        @{
            NodeName = "*"
            DscSourcePath = "C:\ProgramData\AVD-Automated-Deployment"
        }
    );
    AvdData = @{
        AvdAgentInstallUri = $null
    }
}